import { NextRequest, NextResponse } from 'next/server';
import bcrypt from 'bcryptjs';
import jwt from 'jsonwebtoken';
interface User { id: string; email: string; name: string; passwordHash: string; role: string; enabled: boolean; createdAt: string; createdBy: string; lastLogin?: string; department?: string; allowedSources?: string[]; }
const BUILTIN_ADMIN: User = { id: '1', email: 'deepak.k@red.health', name: 'Deepak Kumar', passwordHash: '$2a$10$S7Zz0gSsxRqSHwLGv0r.XeH/JPiRmg2u1ouwVVlPW39xXF6YxI2BS', role: 'admin', enabled: true, createdAt: '2026-01-01T00:00:00.000Z', createdBy: 'system', department: 'Technology', allowedSources: ['both'] };
function getUsers(): User[] { const all: User[] = []; try { const raw = process.env.USERS_JSON?.trim(); if (raw && raw.startsWith('[')) all.push(...JSON.parse(raw)); } catch {} try { const fs = require('fs'), path = require('path'); const file = path.join(process.cwd(), 'users.json'); if (fs.existsSync(file)) all.push(...JSON.parse(fs.readFileSync(file, 'utf8'))); } catch {} if (!all.some(u => u.email === BUILTIN_ADMIN.email)) all.push(BUILTIN_ADMIN); return all; }
export async function POST(req: NextRequest) { try { const { email, password } = await req.json(); if (!email || !password) return NextResponse.json({ error: 'Email and password required' }, { status: 400 }); const users = getUsers(); const user = users.find(u => u.email.toLowerCase() === email.toLowerCase().trim()); if (!user) return NextResponse.json({ error: 'Invalid credentials' }, { status: 401 }); if (user.enabled === false) return NextResponse.json({ error: 'Your account has been disabled. Contact your administrator.' }, { status: 403 }); const valid = await bcrypt.compare(password, user.passwordHash); if (!valid) return NextResponse.json({ error: 'Invalid credentials' }, { status: 401 }); const token = jwt.sign({ email: user.email, name: user.name, role: user.role, id: user.id }, process.env.JWT_SECRET!, { expiresIn: '8h' }); return NextResponse.json({ token, user: { id: user.id, email: user.email, name: user.name, role: user.role, allowedSources: user.allowedSources || ['both'] } }); } catch (err: any) { return NextResponse.json({ error: 'Auth failed: ' + err.message }, { status: 500 }); } }
